# Scenario B API Cutover Plan
# FR-016 / SC-008

cutover_id: "scenariob-bigbang-2026"
status: IN_PROGRESS
scheduled_at: "2026-04-24T00:00:00Z"
executed_at: "2026-04-24T00:00:00Z"

# Totals derived from legacy-artifacts.yaml
legacy_artifacts_total: 20
legacy_artifacts_removed: 20

# Infrastructure components from infrastructure-reuse.yaml
infra_components_reused:
  - INFRA-001  # Keycloak (IDENTITY, RECONFIGURED)
  - INFRA-002  # PostgreSQL (DATASTORE, AS_IS)
  - INFRA-003  # Redis (CACHE, AS_IS)
  - INFRA-004  # Docker Compose (RUNTIME, RECONFIGURED)

notes: >
  Big-bang cutover: no coexistence between Scenario A API v1 and Scenario B API v2.
  Frontend remains untouched (risk accepted — SC-025).
  Status will be updated to IN_PROGRESS at cutover start and COMPLETED after validation.
