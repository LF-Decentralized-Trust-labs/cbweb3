# relay.compose.yaml — variable contract.
RELAY_CONTAINER_NAME=cbweb3-b-relay
RELAY_PORT=9000
HUB_RPC_PORT=8845
RELAY_NET_PREFIX=relay
RELAY_VOLUME_PREFIX=relay
# Shared X-Relay-Auth credential (local-dev value; override per deployment).
INTERNAL_RELAY_AUTH_SECRET=cbweb3-relay-shared-secret
