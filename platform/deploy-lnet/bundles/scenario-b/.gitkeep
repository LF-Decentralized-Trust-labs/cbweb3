# Scenario B spoke layout (one folder per spoke = dataDir + join bundle):
#   spoke-brazil/spoke-brazil.bundle.yaml
#   spoke-colombia/spoke-colombia.bundle.yaml
# (hub.bundle.yaml lives at ../hub/ — see ../hub/.gitkeep)
