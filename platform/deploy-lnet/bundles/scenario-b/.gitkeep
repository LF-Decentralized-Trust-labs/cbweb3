# Scenario B spoke join bundles.
# spoke-brazil.bundle.yaml   → scp'd from CB Brazil,   consumed by cb1/cb2
# spoke-colombia.bundle.yaml → scp'd from CB Colombia, consumed by cb3/cb4
# (hub.bundle.yaml lives at ../hub/ — see ../hub/.gitkeep)
