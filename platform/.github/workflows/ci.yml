name: ci
on:
  push: { branches: [main] }
  pull_request: { branches: [main] }
jobs:
  show-structure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show repository tree
        run: ls -R
