# cbweb3-platform Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-06-08

## Active Technologies

- Bash (deployment/genesis scripts), Go 1.26+ (`api-gateway`, `payment-orchestrator`), Solidity via Foundry/`forge` (contract redeploy targets — no contract source changes) + Hyperledger Besu 25.8.0 (QBFT consensus), Docker Compose, GNU Make, Foundry (`forge`/`cast`) (001-hub-network-isolation)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash (deployment/genesis scripts), Go 1.26+ (`api-gateway`, `payment-orchestrator`), Solidity via Foundry/`forge` (contract redeploy targets — no contract source changes)

## Code Style

Bash (deployment/genesis scripts), Go 1.26+ (`api-gateway`, `payment-orchestrator`), Solidity via Foundry/`forge` (contract redeploy targets — no contract source changes): Follow standard conventions

## Recent Changes

- 001-hub-network-isolation: Added Bash (deployment/genesis scripts), Go 1.26+ (`api-gateway`, `payment-orchestrator`), Solidity via Foundry/`forge` (contract redeploy targets — no contract source changes) + Hyperledger Besu 25.8.0 (QBFT consensus), Docker Compose, GNU Make, Foundry (`forge`/`cast`)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
