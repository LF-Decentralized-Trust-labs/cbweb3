# Infra (Compose)
KEYCLOAK_CONTAINER_NAME=cbweb3-keycloak
KEYCLOAK_PORT=8081
KEYCLOAK_ENV_OUTPUT_DIR=/opt/keycloak/host/backend/config
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin

POSTGRES_CONTAINER_NAME=cbweb3-postgres
POSTGRES_IMAGE_TAG=17-alpine
POSTGRES_PORT=5432
POSTGRES_USER=default
POSTGRES_PASSWORD=default
POSTGRES_DB_SPOKE_B=cbweb3_spoke_b

REDIS_CONTAINER_NAME=cbweb3-redis
REDIS_IMAGE_TAG=7-alpine
REDIS_PORT=6379

# Domain defaults (spoke-b)
KC_BASE_PATH=http://localhost:8081
KC_REALM=cbweb3-spoke-b
KC_CLIENT_ID=cbweb3-spoke-b-client
DB_NAME=cbweb3_spoke_b
DATABASE_URL=postgres://default:default@localhost:5432/cbweb3_spoke_b?sslmode=disable

# PKI — spoke root CA (used to sign commercial bank CSRs)
# Local dev: generate with `openssl` — see backend/config/pki/README.md
CA_CERT_FILE=/workspace/backend/config/pki/spoke-b-ca.crt
CA_KEY_FILE=/workspace/backend/config/pki/spoke-b-ca.key
# Production (Docker Secrets): uncomment below and comment out the lines above
#CA_CERT_FILE=/run/secrets/ca.crt
#CA_KEY_FILE=/run/secrets/ca.key

# Blockchain — spoke-b Besu node (chain 1339)
# Spoke-B hosts regional contracts: IdentityRegistry, tCeBM token, HTLC, SpokeBridge
# Contract addresses below are populated automatically by `make contracts.sync-addresses` after deployment.
# Local dev reference addresses (genesis deploy via `make deploy.up-with-contracts`):
BLOCKCHAIN_CLIENT=noop
#BLOCKCHAIN_CLIENT=besu
#BESU_RPC_URL=http://spoke-besu-b:8545
#BESU_CHAIN_ID=1339
#CB_PRIVATE_KEY=

# IdentityRegistry — whitelist of approved participants for spoke-b (regional KYC/AML registry)
#PARTICIPANT_REGISTRY_ADDRESS=0x42699a7612a82f1d9c36148af9c77354759b210b

# TokenizedCentralBankMoney — domestic tCeBM token issued by spoke-b central bank
#TOKEN_ADDRESS=0xa50a51c09a5c451c52bb714527e1974b686d8e77

# HashTimeLockedContract — atomic cross-chain swap escrow for spoke-b
#HTLC_ADDRESS=0x9a3dbca554e9f6b9257aaa24010da8377c57c17e

# SpokeBridge — bridge contract that connects spoke-b to the hub chain
#SPOKE_BRIDGE_ADDRESS=0x9b8397f1b0fecd3a1a40cdd5e8221fa461898517

# Governance bootstrap — Keycloak subject for the regional Central Bank of spoke B
GOVERNANCE_USER_ID=service-account-cbweb3-spoke-b-client
