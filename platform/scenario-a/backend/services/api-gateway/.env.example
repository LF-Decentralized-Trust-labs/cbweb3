APP_PORT=8080
REQUEST_TIMEOUT_SEC=5
AUTH_GRPC_ADDR=localhost:9091
COMPLIANCE_GRPC_ADDR=localhost:9093
# Set to true when the gateway is served over HTTPS so auth cookies carry the Secure flag.
# Use false for local HTTP development.
COOKIE_SECURE=false
