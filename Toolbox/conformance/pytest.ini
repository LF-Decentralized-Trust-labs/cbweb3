[pytest]
testpaths = tests
markers =
    happy_path: Happy-path conformance tests
    error: Error-case conformance tests
    edge_case: Edge-case conformance tests
    fx_agreement: FX Agreement domain tests
    htlc: HTLC Settlement domain tests
addopts = -v --tb=short
